name: plasterboard_explorer
description: Standalone plasterboard layout and sheet explorer widgets.
publish_to: none

version: 0.1.0

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.0.0
flutter:
  uses-material-design: true
