name: room_editor
description: Reusable plasterboard editor widgets and tooling surfaces.
publish_to: none

version: 0.1.0

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
